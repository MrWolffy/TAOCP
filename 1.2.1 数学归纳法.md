# 1.2 数学准备

## 1.2.1 数学归纳法

> 算法I（构造一个证明） 给定一个正整数n，这个算法将输出P(n)为真的一个证明。
> 
> - I1. [证明P(1)] 置$$k \leftarrow 1$$，输出P(1)的一个证明。
> - I2. [k = n?] 如果k = n，算法终止，所要求的证明已被输出。
> - I3. [证明P(k+1)] 输出“如果所有的P(1), ..., P(k)都为真，则P(k+1)为真“的证明，也输出“我们已经证明P(1), ..., P(k)；因此P(k+1)为真”。
> - I4. [k加1] k加1并转到步骤I2。
> 

<div></div>

> 算法E（扩充的欧几里得算法）给定两个正整数m和n，我们计算它们的最大公因子d和两个整数a和b，使得$$am + bn = d$$。
> 
> - E1. [初始化] 置$$a' \leftarrow b \leftarrow 1, a \leftarrow b' \leftarrow 0, c \leftarrow m, d \leftarrow n$$。
> - E2. [除] 设q和r分别是c除以d的商和余数。（我们有$$c = qd + r$$，且$$0 \le r < d$$。）
> - E3. [余数为0?] 如果$$r = 0$$，算法终止；在此情况下，我们如愿地有$$am + bn = d$$。
> - E4. [循环] 置$$c \leftarrow d, d \leftarrow r, t \leftarrow a', a' \leftarrow a, a \leftarrow t - qa, t \leftarrow b', b' \leftarrow b, b \leftarrow t - qb$$，并返回E2。

### 习题

> 1

- I1. [证明P(0)] 置$$k \leftarrow 0$$，输出P(0)的一个证明。
- I2. [k = n?] 如果k = n，算法终止，所要求的证明已被输出。
- I3. [证明P(k+1)] 输出“如果所有的P(0), ..., P(k)都为真，则P(k+1)为真“的证明，也输出“我们已经证明P(1), ..., P(k)；因此P(k+1)为真”。
- I4. [k加1] k加1并转到步骤I2。

> 2

分母有$$a^{(n-1)-1}$$即$$a^{n-2}$$，所以至少要再证明一个初始条件，即当$$n=2$$时成立。但当$$n=2$$时，$$a^{n-1} = a$$，显然不一定等于1，因此证明错误。

> 3

当$$n = 1$$时，左边应该是$$\frac{1}{0 \times 1}$$，所以初始条件就是错的。

> 4

$$F_n \ge \phi^{n-2}, \phi = \frac{1 + \sqrt{5}}{2}$$

$$\phi^{-1} = \frac{2}{1 + \sqrt{5}} = \frac{\sqrt{5} - 1}{2}$$，所以当$$n=1$$时，有$$F_1 = 1 > \phi^{-1}$$。当$$n = 2$$时，又有$$F_2 = 1 = \phi^0 = 1$$。

设命题对于1, 2, ..., n为真，对于n+1有$$F_{n+1} = F_n + F_{n-1} \ge \phi^{n-3} + \phi^{n-2} = \phi^{n-3}(1 + \phi) = \phi^{(n+2)-1}$$，成立。

> 5
 
2是素数，所以$$n=2$$成立。

设命题对于2, 3, ..., n为真。对于n+1，如果n+1为素数显然成立。如果n+1不为素数，则按照定义其存在其他因数，即存在$$a_1$$使得$$n+1 = a_1b$$，其中$$a_1 > 1$$。显然$$b < n + 1$$即$$b \le n$$，因此命题对于b为真，即$$b = a_2 a_3 \cdots a_m$$。所以$$n+1 = a_1 a_2 \cdots a_m$$，成立。

> 6

执行E4之前等式(6)成立：$$a'm + b'n = c, am + bn = d$$。

执行E4之后，等式变为$$am + bn = d, (a' - qa)m + (b' - qb)n = r$$。

前者显然成立，后者有$$(a' - qa)m + (b' - qb)n = a'm + b'n - q(am + bn) = c - qd = r$$。

> 7
 
$$1^2 = 1, 2^2 - 1^2 = 3, 3^2 - 2^2 + 1^2 = 6, 4^2 - 3^2 + 2^2 - 1^2 = 10$$，猜想$$\sum_{i=1}^{n}{(-1)^{n-i}i^2} = \frac{(n+1)n}{2}$$。

证明：$$n=1$$时显然成立。

设命题对于2, 3, ..., n为真。对于n+1，有$$\sum_{i=1}^{n+1}{(-1)^{n-i}i^2} = (n+1)^2 - \sum_{i=1}^{n}{(-1)^{n-i}i^2} = n^2 + 2n + 1 - \frac{(n+1)n}{2} = \frac{2n^2 + 4n + 2 - n^2 - n}{2} = \frac{(n+2)(n+1)}{2}$$，成立。

> 8

(a)

原命题为$$n^3 = \sum^{n}_{i=1}{[n(n-1) + 2i - 1]}$$。

对于n+1，$$(n+1)^3 = n^3 + 3n^2 + 3n + 1 = \sum^{n}_{i=1}{[n(n-1) + 2i - 1]} + 3n^2 + 3n + 1 $$
$$= \sum^{n}_{i=1}{[(n+1)n + 2i - 1]} + [n(n+1) + 2(n + 1) - 1] = \sum^{n+1}_{i=1}{[n(n-1) + 2i - 1]}$$

(b)
 
对于n+1，有$$\sum_{i=1}^{n+1}{i^3} = (n+1)^3 + \sum_{i=1}^{n}{i^3} = \sum^{n+1}_{i=1}{[(n+1)n + 2i - 1]} + (\sum_{i=1}^{n}{i})^2$$
$$ = (\sum_{i=1}^{n}{i})^2 + (n+1)^2n + (n+2)(n+1) - (n+1) = (1 + 2 + \cdots + n)^2 + 2(n+1)\sum_{i=1}^{n}{i} + (n+1)^2$$

> 9
 
对于n+1，有$$(1-a)^{n+1} = (1-a)^n(1-a) \ge (1-na)(1-a) = 1 - (n+1)a + na^2 \ge 1 - (n+1)a$$。

> 10

$$n=10$$时，$$2^n = 1024 > 1000 = n^3$$，成立。

对于n+1，有$$2^{n+1} = 2 \times 2^n > 2 \times n^3 = n^3 + (n-3+3)n^2 = n^3 + 3n^2 + (n-3)n^2 > n^3 + 3n^2 + 4n^2$$

$$> n^3 + 3n^2 + 3n + 1 = (n+1)^3$$

> 12

- E0. [确保$$m \ge n$$] 如果$$m<n$$，交换$$m \leftrightarrow n$$。
- E1. [求余数] 以n除m并令r为所得余数。（我们将有$$0 \le r < n$$）
- E2. [余数为零？] 若r = 0，算法结束，n即为答案。
- E3. [减少] 置$$m \leftarrow n, n \leftarrow r$$，并返回步骤E1。

定义无理数除法即可。即$$m=qn+r$$，其中q是整数，$$0 \le r < n$$。

> 13

每一次循环至少使得d减少1，因此循环的次数不会超过n次。

> 15

(a): 不满足 iii)。

(b): 绝对值小于

(c): 不是。

(d): 是。

(e): 是。

(f):






